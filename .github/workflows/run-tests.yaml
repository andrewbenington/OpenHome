name: Run Tests

permissions:
  contents: read

on:
  pull_request_target:
  push:
    branches:
      - main

jobs:
  prettier:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install NodeJS
        uses: actions/setup-node@v4

      - name: Install Dependencies
        run: npm ci

      - name: Build WASM
        run: npm run wasm

      - name: Run Tests
        run: npm run test
